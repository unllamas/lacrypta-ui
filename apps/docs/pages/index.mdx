import React, { useState } from 'react';
import { Flex, Button, Container, Divider, Text } from '@unllamas/lacrypta-ui';
import { Hero } from '../components/Landing/Hero';
import { Preview } from '../components/Landing/Preview';
import { DashboardPreview } from '../components/Landing/DashboardPreview';
import { PosPreview } from '../components/Landing/PosPreview';

import { appTheme } from '@/config/theme';

export default () => {
  const [preview, setPreview] = useState('wallet')

  return (
    <Flex direction="column" flex={1}>
      <Hero />
      <Container size="lg">
        <Flex gap="sm" align="center">
          <Text size="sm" color={appTheme.colors.gray[500]}>
            Ejemplos:
          </Text>
          <div>
            <Button variant={preview === 'wallet' ? 'bezeledGray' : 'borderless'} size="sm" onClick={() => setPreview('wallet')}>
              Billetera
            </Button>
          </div>
          <div>
            <Button variant={preview === 'pos' ? 'bezeledGray' : 'borderless'} size="sm" onClick={() => setPreview('pos')}>
              Pos
            </Button>
          </div>
          <div>
            <Button variant="borderless" size="sm" disabled={true}>
              Tickets
            </Button>
          </div>
          <div>
            <Button variant="borderless" size="sm" disabled={true}>
              Tienda
            </Button>
          </div>
        </Flex>
      </Container>
      <Divider y={24} />
      <Flex justify="center">
        <Preview screen="md">
          {preview === 'wallet' && <DashboardPreview />}
          {preview === 'pos' && <PosPreview />}
        </Preview>
      </Flex>
    </Flex>
  );
};
