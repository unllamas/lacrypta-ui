import { Callout } from 'nextra/components';
import { Card, Button, Flex, Drawer, Container, Divider, Input, useDisclosure, Cancel, Icon } from '@unllamas/lacrypta-ui';

# Drawer

## Import

```js copy
import { Drawer } from '@unllamas/lacrypta-ui';
```

## Usage

<br />

export function PreviewDrawer({ children }) {
  const { isOpen, onOpen, onClose } = useDisclosure()

  return (
    <Flex align='center' justify='center' style={{ minHeight: '668px' }}>
      <div>
        <Button onClick={onOpen}>Open Drawer</Button>
      </div>
      <Drawer isOpen={isOpen} onClose={onClose}>
        <Input label="Email" placeholder="you@example.com" />
        <Divider y="sm" />
        <Flex gap="sm">
          <Button isBlock variant="bezeledGray" onClick={onClose}>
            Cancelar
          </Button>
          <Button isBlock>Guardar</Button>
        </Flex>
      </Drawer>
    </Flex>
  )
}

<Card variant="outline" spacing={4}>
  <PreviewDrawer>Open Drawer</PreviewDrawer>
</Card>

```js copy showLineNumbers {4,8,9,11}
import { useDisclosure, Drawer, Button } from '@unllamas/lacrypta-ui';

export const Preview = () => {
  const { isOpen, onOpen, onClose } = useDisclosure();

  return (
    <>
      <Button onClick={onOpen}>Open Drawer</Button>
      <Drawer isOpen={isOpen} onClose={onClose}>
        ...
      </Drawer>
    </>
  );
}
```

### Custom title

<br />

export function PreviewCustomTitle({ children }) {
  const { isOpen, onOpen, onClose } = useDisclosure()

  return (
    <Flex align='center' justify='center' style={{ minHeight: '668px' }}>
      <div>
        <Button onClick={onOpen}>Open Drawer</Button>
      </div>
      <Drawer isOpen={isOpen} onClose={onClose} title='Testeando el titulo'>
        <Input label="Email" placeholder="you@example.com" />
        <Divider y="sm" />
        <Flex gap="sm">
          <Button isBlock variant="bezeledGray" onClick={onClose}>
            Cancelar
          </Button>
          <Button isBlock>Guardar</Button>
        </Flex>
      </Drawer>
    </Flex>
  )
}

<Card variant="outline" spacing={4}>
  <PreviewCustomTitle>Open Drawer</PreviewCustomTitle>
</Card>

```js /title/
  <Drawer ... title={'Testeando el titulo'}>
    ...
  </Drawer>
```

### Custom close button

<br />

export function PreviewCustomClose({ children }) {
  const { isOpen, onOpen, onClose } = useDisclosure()

  return (
    <Flex align='center' justify='center' style={{ minHeight: '668px' }}>
      <div>
        <Button onClick={onOpen}>Open Drawer</Button>
      </div>
      <Drawer isOpen={isOpen} onClose={onClose} closeText={<Icon size='md' icon={<Cancel />} />}>
        <Input label="Email" placeholder="you@example.com" />
        <Divider y="sm" />
        <Flex gap="sm">
          <Button isBlock variant="bezeledGray" onClick={onClose}>
            Cancelar
          </Button>
          <Button isBlock>Guardar</Button>
        </Flex>
      </Drawer>
    </Flex>
  )
}

<Card variant="outline" spacing={4}>
  <PreviewCustomClose>Open Drawer</PreviewCustomClose>
</Card>

```js /closeText/
import { Cancel } from '@unllamas/lacrypta-ui'

<Drawer ... closeText={<Cancel />}>
  ...
</Drawer>
```